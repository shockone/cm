'use strict';


describe('controllers', function(){
  beforeEach(module('contactManager.controllers'));


  it('should ....', inject(function() {
    //spec body
  }));

  it('should ....', inject(function() {
    //spec body
  }));
});
